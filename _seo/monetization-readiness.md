# 収益化準備監査の記録

公開サイトには出ない(Jekyll は `_` で始まるフォルダを出力しない)。同じ監査を繰り返さないための記録。

## 第1回(2026-09-26、AdSense 審査待ち期間)

### 結論

- A(今すぐ修正): 0件 — サイトのコード変更なし
- B(承認後に実施): 6件
- C(HOLD): 3件

### 現状(確認済み)

| 項目 | 状態 |
|---|---|
| 審査コード | 公開HTML 16ファイルすべての `<head>` に adsbygoogle.js が1回ずつ(二重読み込みなし)。`google-adsense-account` メタも各1回。publisher ID は1種類で ads.txt と一致 |
| 手動広告枠 | なし(`<ins class="adsbygoogle">` 0個)。自動広告のみで配信できる状態 |
| ads.txt | 本番 200・text/plain・改行終端・リポジトリと一致。`www.` と `http://` は本番URLへ 301。build-public.ps1 の公開対象に含まれる |
| robots.txt | 全体を許可(広告クローラーもブロックしていない) |
| プライバシーポリシー | GA4・AdSense・Cookie・第三者配信・広告設定/aboutads でのオプトアウト・免責・改定日(2026-09-24)を記載。記載のリンク先は有効 |
| お問い合わせ | Google フォーム(有効) |
| サイトについて / 運営者情報 | あり |
| 導線 | 全ページの footer から サイトについて / 運営者情報 / プライバシーポリシー / お問い合わせ の4つ。いずれも index 対象・sitemap 掲載 |
| 解析タグ | GA4(gtag)を全ページで読み込み。プライバシーポリシーの記載と一致 |

### B: AdSense 承認後に行うこと

依存関係の順に記載。

1. **自動広告を有効化(AdSense 管理画面・ユーザー操作)**
   - 役割: コード変更なしで最初の収益を出す。サイト側の準備は完了済み。
2. **自動広告の除外設定(管理画面・ユーザー操作)** — 1 の直後
   - 対象: `/404.html`、`/search.html`(結果が無い画面になりうる)、存在しない名前の `game.html` / `streamer.html`(noindex の「見つかりません」画面)。
   - 役割: コンテンツの無い画面への広告表示(ポリシー上避けるべき配置)を防ぐ。管理画面で除外しにくい場合は、404.html から広告スクリプトを外すコード変更で代替できる(小規模)。
3. **欧州(EEA・英国・スイス)向け同意メッセージの設定(管理画面「プライバシーとメッセージ」・ユーザー操作)**
   - 役割: 対象地域の閲覧者への広告配信条件を満たす。閲覧者の大半は日本と想定され、収益への影響は小さいが、設定は管理画面だけで済む。
4. **自動広告有効化後の CLS・表示の再計測(サイト側の作業)** — 1 の1〜2週間後
   - フェーズ⑤で CLS を PC 0〜0.021・スマホ 0〜0.034 に改善済み。自動広告が本文中に後から挿入されると、これが悪化しうる。
   - 主要ページ(トップ・ゲーム詳細・VTuber詳細・一覧)を広告ありで計測し、CLS が 0.1 を超える、または操作ボタンの近くに広告が入る場合は、管理画面で本文内の広告形式を絞るか、5 の手動枠に切り替える。
5. **手動広告枠の追加(サイト側の作業)** — 4 の結果、自動広告の配置が悪い / CLS が悪化する場合のみ
   - 候補位置(いずれも本文の区切り。ナビゲーション・ボタンの直近とファーストビューは避ける):
     - ゲーム詳細: 「再生リストを探す」(一覧+ページ送り)の後、「このゲームを実況しているVTuber」の前
     - VTuber詳細: 「すべての再生リスト」(一覧+ページ送り)の後、「人気実況」の前
     - トップ: 「人気のゲーム」と「最近更新された再生リスト」の間
     - ランキング・新着・再生リスト一覧: 表とページ送りの後
   - 実装方針: 枠ごとに min-height で表示前から高さを確保し(フェーズ⑤と同じ考え方)、未配信時に大きな空白が残らない高さにする。1画面に1枠まで。
6. **ads.txt の状態確認(管理画面・ユーザー操作)**
   - 承認直後は管理画面に「ads.txt ファイルの問題」と出ることがあるが、クロール待ちの場合は数日で解消する。サイト側のファイルは正常。

### C: HOLD

- singles.html が noindex なのに sitemap に載っている(フェーズ⑥で P2)。広告・審査には無関係で、審査中の変更は避ける。
- 詳細ページの追加加筆。ゲーム詳細・VTuber詳細は、固有の一覧・統計・関連リンク・固有の description を持ち、広告を置いても主コンテンツが残る。薄さを理由にした大量加筆はしない。
- 開発用ファイル(.ps1・テスト用JS)が公開状態。リンクされておらず、審査・配信への影響は確認されていない。

### 広告戦略(技術面)

- 推奨: **C(自動広告+必要な箇所だけ手動広告)を、自動広告から段階的に始める**。
- 理由:
  - 詳細ページ・一覧ページは縦に長く、自動広告が本文の区切りに配置しやすい構造。コード変更なしで開始でき、管理コストが最も低い。
  - 一方、本文は JS で後から描画されるため、自動広告の挿入位置・タイミングによっては CLS が悪化しうる。計測して問題がある箇所だけ、高さを確保した手動枠に置き換えるのが安全。
  - 手動のみ(B)は、全テンプレートへの枠追加・高さ設計・テストが必要で、承認前に行うのは不可逆・大規模な変更になる。
